<!DOCTYPE html>
<html>
<body>
  <h1>Config Debug</h1>
  
  <h2>Full Config:</h2>
  <pre>{{ config|tojson(indent=2) }}</pre>
  
  <h2>Footer Check:</h2>
  <p>
    nearby_city exists? <strong>{{ 'YES' if config.footer.nearby_city is defined else 'NO' }}</strong><br>
    Value: <strong>{{ config.footer.nearby_city|default('(empty)') }}</strong>
  </p>
</body>
</html>